import type { Metadata } from "next"; import { notFound } from "next/navigation"; import { isLocale,t } from "@/lib/i18n"; import { LocaleNavbar } from "@/components/i18n/locale-navbar"; import { LocaleFooter } from "@/components/i18n/locale-footer"; import { LocalizedRecruitmentForm } from "@/components/i18n/localized-recruitment-form";
export async function generateMetadata({params}:{params:Promise<{locale:string}>}):Promise<Metadata>{const {locale}=await params;if(!isLocale(locale))return{};return {title:`${t[locale].navJoinLink} | ELIVA SCHOOL`,description:t[locale].recruitmentLead,alternates:{canonical:`/${locale}/rejoignez-nous/formateur`,languages:{fr:"/fr/rejoignez-nous/formateur",ar:"/ar/rejoignez-nous/formateur"}}};}
export default async function Recruit({params}:{params:Promise<{locale:string}>}){const {locale}=await params;if(!isLocale(locale))notFound();const s=t[locale];return <main><LocaleNavbar locale={locale} path={`/${locale}/rejoignez-nous/formateur`}/><section className="bg-ink py-24 text-white"><div className="section-shell"><p className="eyebrow text-gold">{locale==="fr"?"REJOIGNEZ LE RÉSEAU ELIVA":"انضم إلى شبكة ELIVA"}</p><h1 className="mt-5 max-w-4xl text-5xl font-semibold sm:text-7xl">{s.recruitmentTitle}</h1><p className="mt-7 max-w-3xl text-lg leading-8 text-white/70">{s.recruitmentLead}</p><a href="#candidature" className="mt-8 inline-flex rounded-full bg-gold px-6 py-3 font-semibold text-ink">{s.apply}</a></div></section><section id="candidature" className="section-shell grid gap-12 py-20 lg:grid-cols-[.8fr_1.2fr]"><div><h2 className="text-4xl font-semibold">{locale==="fr"?"Valorisez votre expertise.":"طوّر أثر خبرتك."}</h2><p className="mt-5 leading-7 text-ink/65">{locale==="fr"?"Candidature conservée dans l’archive interne ELIVA.":"يتم حفظ الترشح في أرشيف ELIVA الداخلي."}</p></div><LocalizedRecruitmentForm locale={locale}/></section><LocaleFooter locale={locale}/></main>; }
