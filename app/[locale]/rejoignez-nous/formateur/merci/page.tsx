import Link from "next/link"; import { notFound } from "next/navigation"; import { isLocale,t } from "@/lib/i18n"; import { LocaleNavbar } from "@/components/i18n/locale-navbar"; import { LocaleFooter } from "@/components/i18n/locale-footer";
export default async function RecruitThanks({params}:{params:Promise<{locale:string}>}){const {locale}=await params;if(!isLocale(locale))notFound();return <main><LocaleNavbar locale={locale} path={`/${locale}/rejoignez-nous/formateur/merci`}/><section className="section-shell py-28"><p className="eyebrow text-gold-dark">{t[locale].navJoin}</p><h1 className="mt-5 text-5xl font-semibold sm:text-7xl">{t[locale].success}</h1><p className="mt-7 max-w-2xl text-lg leading-8 text-ink/65">{locale==="fr"?"Votre profil a été ajouté à notre réseau interne de formateurs.":"تمت إضافة ملفك إلى شبكة المدربين الداخلية."}</p><Link href={`/${locale}`} className="mt-8 inline-flex rounded-full bg-ink px-6 py-3 font-semibold text-white">{t[locale].home}</Link></section><LocaleFooter locale={locale}/></main>; }
