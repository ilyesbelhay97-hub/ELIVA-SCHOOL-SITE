import { notFound } from "next/navigation"; import { isLocale,t } from "@/lib/i18n"; import { LocaleNavbar } from "@/components/i18n/locale-navbar"; import { LocaleFooter } from "@/components/i18n/locale-footer";
export default async function About({params}:{params:Promise<{locale:string}>}){const {locale}=await params;if(!isLocale(locale))notFound();return <main><LocaleNavbar locale={locale} path={`/${locale}/a-propos`}/><section className="bg-ink py-24 text-white"><div className="section-shell"><p className="eyebrow text-gold">{t[locale].navAbout}</p><h1 className="mt-5 max-w-4xl text-5xl font-semibold sm:text-7xl">{locale==="fr"?"Une école tournée vers les compétences utiles.":"مدرسة تركز على المهارات المفيدة."}</h1><p className="mt-7 max-w-2xl text-lg leading-8 text-white/70">{locale==="fr"?"ELIVA SCHOOL crée des parcours pratiques, humains et ambitieux pour transformer l’apprentissage en opportunités concrètes.":"تقدم ELIVA SCHOOL مسارات عملية وإنسانية وطموحة لتحويل التعلم إلى فرص حقيقية."}</p></div></section><LocaleFooter locale={locale}/></main>; }
